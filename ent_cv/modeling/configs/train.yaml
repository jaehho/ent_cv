# train config
# Usage: uv run -m ent_cv.modeling.train

# required
dataset_yaml: /mnt/data/ent_cv/datasets/dataset_batch2/data_with_val.yaml
model: yolo26x

# optional (defaults shown)
epochs: 200
batch: -1
imgsz: 1024
rect: true
scale: 0.0
device: 0
