# tune config
# Usage: uv run -m ent_cv.modeling.tune

# required
data: /mnt/data/ent_cv/datasets/dataset_batch2/data_with_val.yaml
model: yolo26x

# optional (defaults shown)
epochs: 30
iterations: 100
imgsz: 640
batch: -1
device: 0
optimizer: AdamW
